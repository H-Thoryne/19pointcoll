import styled from 'styled-components';

const Name = styled.div`
  font-size: 14px;
  font-weight: bold;
  pointer-events: none;
  user-select: none;
  text-align: center;
  margin: 10px 0;
`;

export default Name;